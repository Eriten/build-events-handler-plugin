<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="${%Groovy String}" field="groovyString">
    <f:textbox default = "${descriptor.getGroovyString(instance)}" placeholder = "${descriptor.getGroovyString(instance)}"/>
  </f:entry>
</j:jelly>
